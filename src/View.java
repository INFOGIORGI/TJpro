import javax.swing.*;
import java.awt.*;

public class View extends JFrame
{
    //Attributi

    View(String titolo){ 
        super(titolo);

        //Inizializza pannello inferiore

        //Inizializza pannello centrale
        
        //Operazioni finali
        setSize(300,200);
        setResizable(false);
        setLocationRelativeTo(null);
        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        
        setVisible(true);
    }

}
